// 10) Create a class Calculator with method multiply (int, int) and multiply (double, double) to demonstrate method overloading.  

class Calculator {

    int multiply(int a, int b) {
        return a * b;
    }

    double multiply(double a, double b) {
        return a * b;
    }

    public static void main(String[] args) {
        Calculator calc = new Calculator();

        System.out.println("Multiplication of 5 and 4 (int): " + calc.multiply(5, 4));
        System.out.println("Multiplication of 3.5 and 2.2 (double): " + calc.multiply(3.5, 2.2));
    }
}
